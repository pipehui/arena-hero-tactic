# Copy to /etc/arena-hero/arena-hero.env and keep mode 0600.
# Quotes are recommended if a value contains shell-significant characters.
ARENA_HERO_API_KEY=replace-with-your-api-key
ARENA_HERO_LOG_DIR=/var/lib/arena-hero/logs
NO_PROXY=api.arenahero.io

